{
  "name": "openwts",
  "version": "0.1.0",
  "description": "🌿 Isolated worktrees for opencode. Spin up, switch, run, and clean up git worktrees with zero friction.",
  "keywords": ["git", "worktree", "opencode", "cli", "productivity"],
  "homepage": "https://github.com/atpaawej/openwts",
  "repository": {
    "type": "git",
    "url": "https://github.com/atpaawej/openwts.git"
  },
  "license": "MIT",
  "author": "atpaawej",
  "type": "module",
  "bin": {
    "openwts": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsx src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
